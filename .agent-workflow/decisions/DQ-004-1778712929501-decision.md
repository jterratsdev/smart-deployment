# Decision DQ-004: Keep target-org dynamic query checks isolated from deployment execution

- Status: accepted
- Owner: architect

## Context

DQ-004 needs to determine whether dangling dynamic-query CustomField edges are already installed in the target org without turning wave validation into deploy validation.

## Decision

Add DynamicQueryTargetValidator with an injected lookup and a separate SfCliMetadataLookup that queries FieldDefinition through sf data query.

## Consequences

DQ-005 can decide how to block unsafe waves using structured missingFields without embedding sf CLI calls in commands or graph code.
