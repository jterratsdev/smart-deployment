{
  "summary": "Validate the local wave plan without deploying",
  "examples": [
    "<%= config.bin %> <%= command.id %> --source-path force-app",
    "<%= config.bin %> <%= command.id %> --source-path force-app --use-ai"
  ],
  "flags.source-path.summary": "Path to the Salesforce project to validate",
  "flags.use-ai.summary": "Use configured provider AI validation when available",
  "flags.scope-commits.summary": "Comma-separated commit SHAs or refs to include in validation",
  "flags.scope-manifest.summary": "Path to a JSON commit or story scope manifest",
  "flags.scope-manifest-output.summary": "Write the resolved story scope manifest for CI review",
  "flags.scope-manifest-output.description": "Creates a deterministic JSON contract containing commits, changed files, included metadata, dependency-retained metadata, explicitly included metadata, and ignored metadata."
}
